export interface Scheme {
    id: number,
    name: string,
    groupOfTimes: number,
    groupInterval: number, // 每组间隔 min
    frequency: number,   
    every_cycle_times: number, 
    description: string,
    background_music: string,
    creat_date: string,
  }